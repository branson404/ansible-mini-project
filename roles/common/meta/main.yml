galaxy_info:
  author: Mohammed Shafi A
  description: Simple common role to setup basic stuff on ubuntu servers
  license: MIT
  min_ansible_version: 2.9
  platforms:
    - name: Ubuntu
      versions:
        - noble
  galaxy_tags:
    - common
    - ubuntu
    - setup
    - ssh_keys
    - basic configuration
  company: Hogg Networks

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
