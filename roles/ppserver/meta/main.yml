galaxy_info:
  author: Mohammed Shafi A
  description: Todo app server implementation on ubuntu servers
  license: MIT
  min_ansible_version: 2.9
  platforms:
    - name: Debian
      versions:
        - noble
  galaxy_tags:
    - appserver
    - debian
    - setup
    - ssh_keys
    - APP
  company: Hogg Networks

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
