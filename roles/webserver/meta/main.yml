galaxy_info:
  author: Mohammed Shafi A
  description: Configuring webserver role on ubuntu servers
  license: MIT
  min_ansible_version: 2.9
  platforms:
    - name: Debian
      versions:
        - noble
    - name: RedHat
      versions:
        - centos8
  galaxy_tags:
    - webserver
    - ubuntu
    - setup
    - ssh_keys
    - web
  company: Hogg Networks

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
